{
  "name": "north-star-mcp",
  "version": "1.0.0",
  "description": "MCP server that keeps AI assistants focused on the master plan and prevents scope creep",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build",
    "test": "node --test"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "project-management",
    "alignment",
    "scope-management"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}